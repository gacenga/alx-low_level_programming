#include "main.h"
/**
* mul - multiplies two integers
*
* @a:integer to be multiplied
* @b:integer to multiply by
*
* Return:0 Always
*/
int mul(int a, int b)
{
int c;
c = a * b;
_putchar(c + '0');
return (0);
}
