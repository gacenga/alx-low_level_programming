#include "main.h"
/**
* main - checks for alphabet character
*
* Return: 0 Always
*/
int main(void)
{
int _abs(int s);
return (0);
}
/**
* _abs - checks for alphabet character
*
* @s:character being checked
*
* Return: 0 Always
*/
int _abs(int s)
{
if (s >= 0)
{
_putchar(s);
}
else if (s < 0)
{
_putchar(-s);
}
return (0);
}
