#include "main.h"
/**
* add - adds two integers and returns the result
*
* @x:integer to add
* @y:integer to add
*
* Return: result
*/
int add(int x, int y)
{
int result;
result = x + y;
return (result);
}
