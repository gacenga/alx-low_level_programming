#include <stdio.h>
/**
* main - prints all single digit numbers of base 10 starting from 0
*
* Return: 0 Always (Success)
*/
int main(void)
{
int myNumber = 0;
while (myNumber <= 9)
{
putchar(myNumber);
myNumber++;
}
putchar('\n')
return (0);
}
